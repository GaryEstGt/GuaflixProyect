
@model IEnumerable<Guaflix.Models.Pelicula>

@{
    ViewBag.Title = "buscar";
}

<h2>buscar</h2>
@using (Html.BeginForm("buscar", "Catalogo", FormMethod.Post))
{
   @Html.DropDownList("Eleccion", new SelectList(ViewBag.SelectedList, "Value", "Text"), new { @class = "form-control" })
        <h4>Primer Parametro</h4>
        <p>@Html.TextBox("filter")</p>
        <h4>Segundo Parametro</h4>
        <p>@Html.TextBox("filter2")</p>
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" value="Buscar" class="btn btn-default" />
        </div>
}
<p>



</p>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.type)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.year)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.genre)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.type)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.year)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.genre)
            </td>
        </tr>
    }
    @Html.ActionLink("Regresar","Index")
</table>
